# ==========================================================================
# .cnb.yml — CNB 云原生构建流水线
#
# 用于在 CNB 平台"直接部署"户晨风语录站点。
# 站点基于 Vite + Vue 3 构建，本流水线负责：
#   1. 构建 —— 安装依赖并用 Vite 打包到 dist/
#   2. 公网部署 —— 通过腾讯云 COS 对象存储 + 静态网站托管对外提供服务
#   3. 同步代码 —— 将代码自动推送到 GitHub 同名仓库（XiGou/hu-chenfeng）
#
# 【COS 部署说明】
#   部署到 COS 需要配置以下环境变量（可在 CNB「仓库设置 → 流水线环境变量」
#   或「个人环境变量」中设置，或通过密钥仓库 imports 导入）：
#     COS_SECRET_ID  腾讯云 API 密钥 ID
#     COS_SECRET_KEY 腾讯云 API 密钥 Key
#     COS_BUCKET     存储桶，格式如 `bucket-appid.cos.ap-guangzhou.myqcloud.com`
#     COS_REGION     地域，如 `ap-guangzhou`
#   未配置 COS 凭证时，流水线仅执行「构建校验」，部署步骤自动跳过。
#
# 【GitHub 同步说明】
#   通过 imports 引用密钥仓库 `secret-env` 中的 `secret-env.yml` 文件，
#   该文件包含 GitHub PAT 与用户名（变量名支持 GIT_ACCESS_TOKEN /
#   GITHUB_PAT / GH_PAT / GH_TOKEN / GITHUB_TOKEN / GITHUB_ACCESS_TOKEN），
#   用于将代码自动推送到 GitHub 同名仓库。用户名支持 GIT_USERNAME 设置。
# ==========================================================================

main:
  push:
    - name: 构建并部署站点
      lock:
        key: deploy-static-site
        wait: true
        cancel-in-wait: true
      docker:
        image: node:20
      imports:
        - https://cnb.cool/gotham-xigou/secret-env/-/blob/main/secret-env.yml
      stages:
        # ---- 构建 ----
        - name: 安装依赖并构建
          script: |
            set -e
            echo "==> 初始化 submodule（HuChenFeng 参考仓库）"
            git submodule update --init --recursive || echo "警告：submodule 初始化失败，跳过（构建不依赖其内容）"
            echo "==> 安装依赖"
            npm ci
            echo "==> Vite 生产构建"
            npm run build
            echo "==> 校验构建产物"
            for f in dist/index.html; do
              [ -f "$f" ] || { echo "缺少构建产物: $f"; exit 1; }
            done
            echo "==> Vite 构建通过 ✔"

        # ---- 公网部署到对象存储（COS），未配置凭证则跳过 ----
        - name: 部署到对象存储
          if: '[ -n "$COS_SECRET_ID" ] && [ -n "$COS_SECRET_KEY" ] && [ -n "$COS_BUCKET" ]'
          script: |
            set -e
            echo "==> 安装 coscmd"
            pip install -q coscmd

            echo "==> 配置 coscmd"
            coscmd config -a "$COS_SECRET_ID" -s "$COS_SECRET_KEY" \
                          -b "$COS_BUCKET" -r "${COS_REGION:-ap-guangzhou}"

            echo "==> 上传静态站点（递归同步，删除远端多余文件）"
            coscmd upload -rs ./dist/ / --delete --yes

            echo "==> 站点已部署。若已开启 COS 静态网站托管，可访问："
            echo "    https://${COS_BUCKET}/index.html"

        # ---- 同步到 GitHub 同名仓库 ----
        - name: 同步代码到 GitHub
          if: '[ -n "$GIT_ACCESS_TOKEN" ] || [ -n "$GITHUB_PAT" ] || [ -n "$GH_PAT" ] || [ -n "$GH_TOKEN" ] || [ -n "$GITHUB_TOKEN" ] || [ -n "$GITHUB_ACCESS_TOKEN" ]'
          script: |
            set -e
            # 兼容多种环境变量命名
            PAT="${GIT_ACCESS_TOKEN:-${GITHUB_PAT:-${GH_PAT:-${GH_TOKEN:-${GITHUB_TOKEN:-${GITHUB_ACCESS_TOKEN}}}}}}"

            echo "==> 配置 Git 身份"
            git config --global user.name "${GIT_USERNAME:-${GITHUB_ACTOR:-hu-chenfeng-bot}}"
            git config --global user.email "${GITHUB_ACTOR_EMAIL:-bot@users.noreply.github.com}"

            echo "==> 配置 GitHub 远程仓库"
            GITHUB_REPO="XiGou/hu-chenfeng"
            git remote remove github 2>/dev/null || true
            git remote add github "https://x-access-token:${PAT}@github.com/${GITHUB_REPO}.git"

            echo "==> 拉取 GitHub 最新 main（若远端有更新则同步到本地）"
            git fetch github main:refs/remotes/github/main || echo "首次拉取 GitHub 或暂无远端 main，跳过同步"

            echo "==> 将本地 main 同步到 GitHub 最新"
            if git rev-parse --verify github/main >/dev/null 2>&1; then
              if ! git merge-base --is-ancestor github/main HEAD; then
                echo "GitHub main 领先本地，rebase 将本地提交叠加到 GitHub 之上"
                git rebase github/main
              else
                echo "本地已包含 GitHub 最新提交，无需 rebase"
              fi
            fi

            echo "==> 推送代码到 GitHub（冲突已通过拉取 + rebase 解决，非 force）"
            git push github HEAD:main

            echo "==> 代码已同步到 GitHub ✔"
